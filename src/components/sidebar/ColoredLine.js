import React from 'react';

const ColoredLine = ({ color }) => (
  <hr
      style={{
          color: 'a4a4a4',
          'background-color': '155478',
          height: 4
      }}
  />
);

export default ColoredLine;